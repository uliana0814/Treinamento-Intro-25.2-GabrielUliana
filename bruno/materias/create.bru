meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{folder}}
  body: json
  auth: none
}

body:json {
  {
    "name": "MENGAO!!!! O MAIOR!!",
    "descricao": "Descriçã2 da matéria",
    "cor": "#FF0002",
    "slug": "nova-materia23",
    "imgUrl": ""
  }
}

script:post-response {
  if (res.body && res.body.id) {
    bru.setVar("materiaId", res.body.id);
  } else {
    console.log("No ID found in response");
  }
}
